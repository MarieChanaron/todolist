import { initializeApp } from "./modules/service.mjs";

// Initialize Tasks

// const tasks = generateRandomTodos(10);

// console.log(tasks[0]);

// tasks.forEach(task => {
//     allTasks.push(task);
// });

// retrieveAllTasks();

initializeApp(10);